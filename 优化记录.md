# 优化记录（精简版）

> 更新日期：2026-08-09（由 1-34 节原记录重组精简，完整历程见 `优化记录.backup-20260805.md`；8-07 补充顶栏统一 + 输入框聚焦统一 + 输入框全程可输入 + 编辑器页布局（编辑器优先+抽屉）+ AI 生成进编辑器预览；8-08 补充语言选择搜索式下拉 + 24 语言热门度扩展 + 跨年日期 + 种子数据 12 条 + 弹层定位/焦点环坑 + Monaco 精简/空闲预加载/加载占位 + monaco 0.56 子路径坑 + AI 助手库操作放行（批量 + 收藏夹 CRUD + 改描述/语言，AI 只提议用户确认、不可逆项双重确认）+ 确认卡危险操作红色分级 + 助手首页文案；8-09 补充 AI 页滚动位置恢复（sessionStorage + scroll 事件保存，KeepAlive 失活钩子读 scrollTop 恒 0 的坑）+ AI 对话持久化改临时存储（数据 localStorage / 临时状态 sessionStorage 分储）+ 详情页删独立 AI 解释/优化、AI 入口收敛到助手（删 AiExplainPanel/AiModifyPanel））
> 技术栈：Vue 3 · TypeScript · Vite · Pinia · Vue Router · Monaco Editor · TailwindCSS · DeepSeek（deepseek-v4-flash）

项目定位：轻量化 AI 代码片段管理平台，核心是"代码保管"，不做正式 IDE。

---

## 一、项目定稿架构（最终态）

### 数据与存储
- localStorage + `watch(deep)` 自动保存 + 一次性种子数据；布尔收藏 → `folderIds` 多对多（旧数据一次性迁移）。
- 过滤：语言 / 收藏夹 / 最近一周；**搜索只匹配标题**（`filteredSnippets`，不区分大小写）。
- 输入上限：收藏夹名 12 字（窄侧边栏放得下）、标题 / 搜索框 50 字。
- 种子示例 12 条（多语言覆盖、日期分散，含跨年示例）；**时间显示跨年带年份**（当年只显 M/D，卡片与 AI 侧 fmtDate 一致）。
- **数据与临时状态分储**：数据（片段 `snippets` / 收藏夹 `folders` / seed 标记）走 localStorage 长期保存；**临时状态（AI 对话 `ai-conversation`、AI 页滚动位置 `ai-scroll`、编辑器草稿 `draft`）统一走 sessionStorage**——刷新、页面跳转/返回都保留，关标签页自动清空，三类策略一致。AI 页滚动位置恢复：`scroll` 事件持续保存 + `onMounted`（刷新）/ `onActivated`（KeepAlive 返回）读取恢复。

### 布局与交互
- 列表 2 列卡片网格（`md(768) → lg(1024)` 断点）；**实时搜索**（输入框直绑 store，弃提交式）；**批量管理渐进式勾选**（复选框常驻 + 底部操作条，无"模式"切换）；排序 3 维 × 2 向（最近更新/创建/首字母，中文用 pinyin-pro 转拼音排序；**方向用文字「新→旧 / A→Z」代替箭头**——↑↓ 有"升序 vs 最新在顶"歧义，用户反复反馈"反了"）。
- 侧边栏分区（动作/浏览/筛选）+ 动作区固定、下方滚动 + 折叠默认全展开（不持久化）+ 筛选自动展开联动；收藏夹名重复就地红字拦截（新增/重命名都校验）；空状态四场景 + 动作出口。
- 详情页定位**阅读页**：正常滚动 + 代码自适应高度（行数×22px，下限 260px）；**AI 能力统一收敛到「AI 助手」入口**（`?snippet` 预置当前片段为对话前提，解释/优化进助手对话），删除独立 AI 解释内联展开与 AI 优化抽屉。
- 键盘可达：Monaco 保留 Tab 缩进、**Esc 作出口**（跳编辑器后首个可聚焦控件）；弹窗 / 气泡全站 Esc。
- **Monaco 精简 + 加载（性能）**：完整 monaco-editor 会拉起 TS/JSON/CSS/HTML 四个语言服务 worker（TS 约 7MB），而片段浏览/编辑不需要 IDE 智能提示 → 用裁剪入口 `src/editor/monaco.ts`（从 editor.main.js 生成：删 4 个语言服务 worker + `languages/definitions` + `monaco-lsp-client`，只留 basic-languages 轻量语法高亮 + 标准编辑器功能），dist 从含 ~9MB workers 降到 5.8MB、不再生成 ts/json/css/html worker；配合 `defineAsyncComponent` + `MonacoLoading` 加载占位（delay 200ms）+ 空闲预加载（`requestIdleCallback` 预热 module cache，兜底 setTimeout 2s），详情页/编辑页首开不再等几秒。
- **编辑器页布局（编辑器优先 + 抽屉）**：新建/编辑片段页整体限宽居中（`max-w-7xl`，宽屏左右留白防代码行过长）；元数据压成顶部两行轻量表单（名称+语言一行、描述一行，GitHub issue 式，无边框输入走 `.plain-input` 豁免）；AI 生成面板作为右列抽屉（360px）与编辑器并排，打开时编辑器收缩、关闭恢复——**不做浮层、不做左列表单列**（两列布局实测横向三块挤，用户明确否掉）；顶栏只留 BrandMark+返回+页面标注，AI 生成按钮在底部操作栏。
- **语言不算"未保存"修改**：`isDirty` 排除 language（改语言不弹离开确认，标题/代码/描述仍弹）；语言仍进草稿（刷新可恢复），返回明确离开时草稿清除。
- **语言选择搜索式下拉（LanguageSelect）**：语言扩展至 24 种按热门度排序（TIOBE + Stack Overflow + GitHub 综合，Python 居首），长列表靠滚动找语言是痛点 → 搜索式下拉（GitHub 语言选择器同款），纯鼠标驱动：点击展开 / 输入即过滤 / 点选，无键盘导航；弹层 Teleport 到 body（防容器 overflow 裁剪）+ fixed 定位视口内收敛（方向按触发框上下可用空间比较、top 不越界、`max-height: calc(100vh-8px)` + 列表 flex-1 自适应兜底），Esc / 点击外部 / 外部滚动 / 窗口 resize 关闭；弹层搜索框打开即自动聚焦，聚焦时豁免全局蓝环（同 base 层内比特异性，见关键坑）。文件导入扩展名识别（fileImport）与 Monaco 高亮映射同步覆盖 24 种。

### 视觉定稿（zinc + GitHub 蓝）
- 骨架 zinc（页面底 zinc-100 → 白卡 → 边框 zinc-200/300 层级），强调色 `github-blue #0969DA` / dark #0A52B8 / light #DDF4FF / light-hover #C8E1FF；语义色仅 red（破坏性）+ diff 红绿。
- **反馈设计：无 toast，就地反馈**——结果本身可见（卡片消失/星标/计数归零）即反馈，破坏性操作由 ConfirmDialog 承担确认；桌面端有空间，GitHub 式。
- 交互时序：收藏夹编辑/新建**气泡化**；操作菜单与 FolderPicker「鼠标离开后 5 秒」自动收起（悬停暂停计时、移出重新计），输入型气泡不自动消失。
- **顶栏全站统一**（h-14 / bg-white / border-zinc-200）：左侧 BrandMark（品牌名 text-xl）+ 返回按钮（icon15 + "返回" text-sm）统一结构——AI 助手页原为沉浸全屏自绘顶栏，现并入同构（补品牌、返回按钮统一、去掉 search icon + 页面标题）；右侧说明/页面标注统一 `text-sm`（首页副标语、详情"片段详情"、编辑"编辑/新建片段"、AI 页说明句），首页副标语原 text-xs 一并提为 text-sm。
- **全局焦点环**：`:focus-visible` 统一 GitHub 蓝外圈（2px 白间隔 + 4px 蓝，`!important`）覆盖按钮/链接/输入框；**无边框透明输入框**（编辑标题/描述、AI 助手输入框）用 `.plain-input` 豁免——无边框参照下蓝环会包成"一大圈方框"突兀，聚焦零变化；有边框/底色输入框（搜索框 / select / textarea / 文件夹输入）蓝环贴边保留；文件夹输入框清理被全局规则覆盖的 focus:ring 死类。
- **AI 助手页首屏**：空态欢迎语（两行字）`absolute inset-0` 铺满滚动区正中居中，下方胶囊输入框接住视线——主入口是输入，一进页面 `onMounted` 自动聚焦；换话题/重新开始保持胶囊下方居中（对话开始后才显形，空态不挤占欢迎语视线）。
- **输入框全程可输入**：AI 思考/回复期间输入框不禁用，可预输入下一条（Enter 在思考中静默忽略、内容保留，回复完成后即可发送）；仅对话达 16 轮上限时禁用。

### AI 助手（最终形态）
- **两级召回层**：本地关键词召回 Top 25 进 prompt（**成本与库规模解耦**，全量进 prompt 在库 100-200 条必爆上下文）。中文 bigram 子串匹配，打分加权（标题×4/描述×3/夹×3/语言×2/代码×0.5）。单条代码上限按候选数自适应（代码总量有界 ≈3000×12）；空库短路直接提示不跑模型。**边界**：库 ≤25 条时最近兜底拉全量 = 全库直进 prompt（无漏召回，模型通读全库）；库超 25 条才真正截 Top 25——此时"关键词命中挤满名额"才可能挤掉"字面零重合但相关"的片段（漏召回）。关键词识别 = 英文整词 + 中文相邻两字 bigram 子串包含，不读语义（"别执行那么频繁"打不出 token，全靠最近兜底）。
- **候选三源合并**：上文搜索结果（排最前，追问"有注释的呢"不丢上文）+ token 命中 + 最近兜底，去重切片。prompt 显式给"最近两轮结果编号并集"，追问只能在范围内筛。
- **本地属性标注**（模型看标注不读代码猜）：候选行标 `（含注释 · 收藏于：X · 创建于 M/D）` / `（无注释 · 未收藏 · …）`——解决"有注释的 python"组合、"我收藏的"、"最近添加的/最新的"三类查询（日期无文本特征，实测无标注时模型返回空/反问）；hasComment 语言感知（CSS 行首 # 是选择器/色值，不算注释）。
- **自主分析**：`assistantTurn` 一次调用 = 候选（编号+语言+标题+AI 描述+收藏夹+代码 ≤3000 字符）+ 历史 + 消息 → 动作 JSON 六选一：`search`（ids+note）/ `summarize`（总结/讲解/对比，ids+text）/ `modify`（改代码，前端本地生成 diff 待确认）/ `operate`（库操作，AI 只提议）/ `ask`（意图模糊澄清）/ `chat`（寒暄）。本地只做数据准备与校验（编号去重/越界、括号配平 JSON 提取）。
- 追问在对话历史累积，**范围约束用最近两轮结果编号并集**（单轮并集挡死"更简单点的"反向回退，实测模型返回空）；**换话题按钮**（`freshSearch` 物理不带历史，分割线仅 UI 展示）；16 轮上限 / 历史 12 条。
- **库操作全部放行，但「AI 只提议、用户确认」**：库操作（删除/重命名/导出/收藏/取消收藏/新建/清空/收藏夹管理/改描述改语言）与代码生成（create）不再物理拦截——AI 一律输出 operate 提议，确认卡展示操作内容，用户确认后才落库。prompt 规则保证不了确定性（实测"写个新代码"有规则仍反问），安全性改靠「提议不执行 + 确认卡分级确认」——AI 提议错了，用户不点确认就不会发生。**批量**：delete/favorite/unfavorite 的 ids 支持多个（"把这几个删了"，确认卡列出片段清单）。**收藏夹 CRUD**：按夹名指代（prompt 注入「当前收藏夹：xx、yy」列表），新增 createFolder / renameFolder（target 旧名）/ deleteFolder / clearFolder。**改描述/语言**：meta（field=description|language）。不可逆项（delete/clear/deleteFolder）确认卡上再弹 ConfirmDialog 双重确认；重名夹/夹不存在由 store 校验报错。
- **确认卡配色分级**：危险/不可逆操作（删除片段/清空片段库/删除收藏夹）确认卡红色警示 + 双重确认；普通操作（重命名/导出/收藏/新建/收藏夹管理/改描述改语言）GitHub 蓝——无风险操作不制造紧张感，与 AI 修改卡蓝调一致。
- **思考过程四步总结**：`reasoning_content` 流式累积（内部用，不直接展示）；结果后模型**二次调用（非流式）**把自由推理忠实整理成固定四步文本（① 分析请求 → ② 梳理依据 → ③ 解读意图 → ④ 构思回应，每步一行），结果出来后折叠「AI 思考过程」展示四步；思考太短（<30 字）/总结失败兜底折叠原文。prompt 强制每步独占一行，展示端正则把 ①②③④ 统一独立成行（模型偶发省略换行/编号）。**主 prompt 的思维链引导同步四步动作中性化**（② 梳理依据覆盖候选片段/收藏夹/对话上文，③ 覆盖全部动作含库操作，④ 含 operate 的 op/value/target/field）——助手不只检索代码，推理措辞不再代码导向。二次总结独立调用、maxTokens 1000、失败静默——绝不影响主流程。
- **等待期四步阶段指示**（展示层）：分析请求→梳理信息→解读意图→构思回应，完成=蓝勾/进行中=蓝点脉冲/未到=灰；store `phase` 状态机驱动（reasoning 非空=解读意图、content 开始输出（onChunk）=构思回应），纯前端不依赖模型输出；等待期只显示阶段进度 + 「正在梳理信息…（已等 N 秒）」（思考中切「正在思考…」），思考原文不实时展示（等结束二次总结成四步）；思考超 20 秒追加「AI 推理较慢，最长约 60 秒」——**不再写"复杂查询约需 30-60 秒"**（属性/对比类简单查询同样触发长推理，归因"复杂"误导用户，见 38）；503 退避单独提示不显示阶段。
- **工程保障**：统一 `chat()` 核心（流式/非流式共用、503 退避 5/10/15s、`AIError` 中文+错误码、全链路 AbortController）；错误码体系（ERR_* 11 类 + status + detail）+ `logAiCall` 诊断日志（localStorage 环形 100 条）；超时 60s 分阶段提示（分析中→思考中→重试中）；失败/超时「重试」按钮；`ensureDescription` 保存后异步生成片段描述（幂等、失败静默）。
- **AI 生成面板（抽屉式 + 进编辑器预览）**：结果**流式填入主编辑器**预览（大字/语法高亮/可编辑），面板只留操作、不再有 360px 小代码区——「应用到编辑器」确认（标题留空则聚焦标题输入框引导用户命名，不做 AI 生成标题）/「重新生成」基于当前描述+语言直接重跑（不重置父暂存 `preAiCode`，放弃仍回最初）/「放弃」清空描述+恢复编辑器内容；语言 `defineModel` 与顶部语言选择下拉双向绑定（防 Monaco 高亮与顶部不一致），生成中禁用、完成态可改（支持换语言重新生成）。

## 二、关键坑（跨领域教训）

- **Transition 须包在控制显隐的父级上**（`v-if` 在 Transition 内部），否则直接卸载不播 leave 动画。
- **弹层关闭用 `@mousedown.self`**（编辑器拖选/复制鼠标滑过遮罩误关）；document capture 监听判"点击外部"时，拖选在气泡内按下、外松开会误关 → 用 mousedown 不用 click。ConfirmDialog 遮罩曾残留 `@click.self` 导致同一问题复现（弹窗内按下、拖到遮罩松开误关）。
- **卡片 hover transform 带弹层一起动**（父级 translate/scale → 弹层位移 → 光标进出循环抖动）→ 弹层打开时禁用卡片 hover 位移。
- **Monaco 吞 Tab/Shift+Tab** → 保留 Tab 缩进，Esc 作键盘出口；判断"下一个控件"用 `container.contains()` 定位 Monaco 内部 textarea 在文档序位置。
- **iconfont js 等 `DOMContentLoaded` 与 Vue 首屏竞态** → svg 抽文件 `?raw` 同步注入（`app.mount` 前）；symbol 内硬编码 `fill` 改 `currentColor`；雪碧图不能 `display:none`（破坏 symbol 解析）。
- **`localeCompare('zh')` ICU 拼音 collation 把汉字拼音段整体排到拉丁段后**（「防抖」按 f 排到 C/V 后）→ pinyin-pro 转拼音与英文统一比较。
- **推理模型 `reasoning_content` 与 content 共用 max_tokens 预算，超长推理挤空 content**（否定/排除语义推理 1000+ token、40-50s；属性/对比类查询"哪个代码量最少"实测推理 3870-9759 字符、17-32s）→ max_tokens 按最坏推理场景预算（4000，2026-08-08 由 2000 升，见 38），空输出重试一次（`chatRetryIfEmpty`）。
- **嵌套 JSON 解析**：非贪婪正则 `\{[\s\S]*?\}` 在首个 `}` 截断 → 括号配平扫描（跳过字符串内引号与 `\` 转义）。
- **整文件 Write 重写后 helper 定义被覆盖**（只剩调用点）→ 重写后必 grep 关键符号完整性 + `vue-tsc` 全量验证。
- **测试结论以真实线上代码为准**：esbuild 打包 src/api/ai.ts + define 注入 `import.meta.env` 测试；复制版脚本会失真（曾误判"疑问句式稳定 0%"，真实 23/23 通过）。用户实测与测试矛盾时以用户为准。

- **`min-h-full` 百分比高度在 flex + overflow 下失效**：`flex-1` 滚动容器的 height 是 flex 算法分配（属性值 auto），子元素 `min-height: 100%` 的百分比基准解析为 auto → 空态容器没撑满、`justify-center` 无效果、内容停顶部。修复：滚动容器加 `relative`，空态容器用 `absolute inset-0`（以 padding box 为基准）铺满再居中，可靠。
- **推理模型 `reasoning_content` 不受 prompt 控制**（想让"思考内容按 ①-④ 四步"不可行）：思维链是自发隐式推理，prompt 命令不了它的格式。三轮实测：抽象指令"按四步分析"→ 模型忽略；具体样例"输出 thinking 字段"→ 模型照做但把 JSON 也塞进 reasoning、content 挤空/畸形（搜索总结功能坏）；格式样例+明确"最终只输出 JSON"→ 时灵时不灵、复杂查询偶发回归（ids 空 / ask 兜底）。结论：推理模型上"思考按格式走"与"content 稳定"冲突；未采纳关闭推理（thinking:disabled）方案（失真实推理、判断质量待测），保留推理模式，思考内容自由、等待期改走展示层阶段指示。后续「四步内容」改走**事后二次总结**（非 prompt 控制）：主请求返回后独立非流式调用，把 reasoning 忠实整理成固定四步（content 专输出 JSON），实测稳定（4/4 步齐全、内容忠实）——prompt 命令不了思维链，但可以事后整理思维链；二次调用独立超时/失败静默兜底原文，不拖垮主流程。
- **fixed 定位弹层要收敛在视口内**：手动算 top 的 fixed 弹层用固定估算高度决定向上/向下开，触发框贴近视口顶/底时 top 越界（负值/超出底部）被 `overflow-hidden` 裁掉，文字错位/重叠 → 方向改按触发框上下**可用空间比较**（空间大的一侧开）、top 收敛在视口内；外层加 `max-height: calc(100vh-8px)` + 列表 `flex-1 min-h-0` 自适应，任何窗口高度不溢出。弹层开着时窗口 resize → fixed 与触发框错位 → 监听 resize 关闭。
- **CSS `!important` + `@layer` 优先级反向**：带 `!important` 时**分层样式优先于不分层样式**（与普通声明相反）→ 在组件 `<style scoped>`（不分层）里写 `box-shadow: none !important` 覆盖全局 `@layer base` 的蓝环 `!important` 根本不生效；必须把例外也放进 `@layer base`，**同层内比选择器特异性**（`.lang-select-pop input:focus-visible` > `:where(...):focus-visible`），`.plain-input` 豁免同理。
- **monaco-editor 0.56 exports map 子路径坑**：`"./*"` → `"./esm/vs/*.js"` 会把 `monaco-editor/esm/vs/...` 错误映射成 `esm/vs/esm/vs/...`（TS2307/TS2882）→ 深层导入改用**无扩展名** `monaco-editor/<相对 esm/vs 的路径>`；CSS 用相对物理路径。monaco 内部无 .d.ts，side-effect import 需在 tsconfig 关 `noUncheckedSideEffectImports`（TS2882）。
- **vite-plugin-monaco-editor 的 `languageWorkers` 配置删不掉语言服务 worker**：语言特性通过 `new Worker(new URL(...))` 自建 worker、绕过 plugin 的 MonacoEnvironment → 想砍 worker 只能裁 monaco import 生成裁剪入口，plugin 配置只影响它自己注入的那几个。
- **KeepAlive 失活钩子里读不到滚动位置（恒 0）**：Vue 3.5 `deactivate` 顺序是**先 `move` 节点进隐藏容器、再在 `queuePostRenderEffect` 里触发 `onDeactivated`**（runtime-core 源码顺序）——元素已脱离文档布局，`scrollTop` 恒为 0；用它保存会把好值覆盖成 0，返回时读 0 跳过恢复 → 白存。对称症状是「刷新恢复正常、点进返回不恢复」：beforeunload 保存时 DOM 还完整，读到真值。**修复：滚动位置改挂滚动容器的 `scroll` 事件持续保存**（事件触发时元素仍在文档内，值可靠），恢复只在进入时做（onMounted / onActivated）。

## 三、AI 助手演进史（压缩版）

- **22 AI 能力升级**：收敛统一 `chat()` 核心 + `AIError` 透传 + AbortController（基础设施沿用至今）；模型 `deepseek-chat` → `deepseek-v4-flash` 正式版。
- **23 两级检索**（意图解析→字面粗召回→精排）：**推翻**——意图解析枚举不完、同义词现场补全不稳、模型没见过完整内容（preview 200 字符截断）。
- **24 极简混合**（属性机械处理 + 语义判断）：点破本质"语义天花板是模型能力，不是字段数量"，属性字段定稿（hasComment/recent/folder/limit/hasTopic）。**推翻**（25 后）。
- **25 AI 助手诞生**：单轮搜索"模型理解错了无补救" → 改成**对话追问澄清**；能力范围明确只做搜索+追问，不做库操作；动作 JSON 三选一；10 轮上限。
- **26 删独立 AI 搜索并并入助手**：单轮失败无补救 → 对话解决，不再维护两个 AI 入口；3 次串行调用 → 2 次提速（动作+意图一次输出）。
- **27 本地重排**：精确极值/顺序（最短/最近）本地算，语义只管相关——**被 28 更进一步推翻**。
- **28 自主分析（定稿）**：删掉整套属性过滤管线（SearchIntent/parseIntent/applyAttributeFilters/semanticJudge 等 ~470 行，ai.ts 830→355），**模型读全库内容自主决策**；新增 AI 自动生成 `description` 字段（代码表达不了的背景）。`assistantTurn` 接口保持不变，演进平滑。
- **30 否定语义挤空修复**：max_tokens 1000→2000 + prompt 规则"否定表达直接 search，不要 ask 反问"。
- **31 体验改进**：等待分阶段提示、503 可见（`onRetry`→`retrying`）、可中断 `sleep`（停止立即生效）、`armTimeout` 重试重置不掐断、重试按钮（`lastUserText` + 先弹 user 消息再重发）。
- **32 换话题**：实测定位模型边界（"有没有渐变背景的"反向理解 0%——把"找 X"理解成"排除 X"）→ **规则 + 按钮双出口**；按钮语义物理可靠（不带历史就不可能被带偏）。教训：规则能提高概率、保证不了确定性。
- **33 规模化**：两级召回层（候选 Top 25、成本解耦）、组合/收藏夹本地标注、错误码 + `logAiCall` 日志、轮次 16 / 历史 12、超时 30s→60s。
- **34 流式推理展示**：`reasoning_content` 透传（关键坑：**只有 onReasoning 没有 onChunk 时请求也要走流式分支**）+ 尾部窗口 + 折叠全文。展示目的修正为过程性反馈——证明在动即可，不必证明想多深。
- **35 补全与加固**：新增 `summarize`（总结/讲解/对比，复用同一候选/编号/解析管线，UI 零改动）；**能力边界本地物理拦截**（库操作 + 代码生成，实测"写个新代码"有 prompt 规则仍反问 → 正则直接拒绝不调模型）；追问改最近两轮编号并集（单轮并集挡死反向回退，实测返回空）；候选日期标注（解决"最近添加的"）；hasComment 语言感知（CSS 例外）；divider 过滤；单条代码上限自适应；空库短路。
- **36 思考过程四步总结**：等待期不再实时透传思考原文（改阶段进度 + 时长）；结果后**二次调用**把自由推理忠实整理成固定四步文本（①-④ 每步一行），折叠「AI 思考过程」展示；prompt 强约束 + 展示端正则规整换行；失败/思考太短兜底折叠原文。（流式 + 显示在回复上方的方案实测后弃用——用户观感不佳，回退折叠样式）
- **37 库操作缺口补齐（批量 + 收藏夹 CRUD + 改描述/语言）**：推倒「能力边界物理拦截」（35 引入），库操作/代码生成**全部放行**——AI 只提议（operate），用户确认后才落库。操作面从"只读分析"扩到库结构操作：批量删除/收藏/取消收藏（ids 多值，确认卡列片段清单）、收藏夹新建/改名/删除/清空（AI 按夹名指代，prompt 注入「当前收藏夹」列表）、改描述/语言（meta，field=description|language）。不可逆项（delete/clear/deleteFolder）确认卡 + 双重确认；重名夹/夹不存在 store 校验报错。验证：esbuild + define 注入 env 真实线上动作解析 4/4 + store 级端到端 22/22（mock localStorage + 真实 Pinia，覆盖批量/夹 CRUD/meta 的执行路径与错误分支）。
- **38 属性/对比查询挤空修复（max_tokens 2000→4000）+ 等待文案诚实化**：实测"找个没有注释的代码 → 哪个代码量最少"链路，第二轮属性查询触发模型超长推理（3870-9759 字符、17-32s），2000 输出预算被 `reasoning_content` 吃满 → content 挤空 → JSON 解析失败 → 兜底 ask"没听明白"。4000 下同查询正常返回合法 JSON（对照实验：esbuild define 覆盖 `ASSISTANT_MAX_TOKENS`）。**结论**：这不是复杂操作，是推理模型对属性/对比类简单查询也会过度推理；预算按最坏推理场景给够，而不是按平均场景。同步：等待期超 20 秒文案"复杂查询约需 30-60 秒"→"AI 推理较慢，最长约 60 秒"（归因"复杂"误导，见 46 行）。代价：仅超长推理的病理查询多烧输出 token，正常查询输出本就短、几乎不变。
- **39 四步总结后台补全（去掉阻塞二次调用）**：二次总结原在 `assistantTurn` 内 `await summarizeThinking` **阻塞**——主回复必须等总结跑完才显示，每轮白等一整轮 API。改为 store 在消息 push 后 `backfillSummary` **后台补全**：主回复立即显示、折叠面板先展示 reasoning 原文、总结到后替换成四步。实现：`assistantTurn` 不再调用/返回总结（`AssistantReply` 移除 thinkingSummary 字段，消息字段保留）；store 独立 AbortController + 30s 超时兜底，新一轮 backfill / reset 作废上一个未完成的总结（同时只一个在飞、防竞态）；经响应式数组取回消息 proxy 再赋值，确保触发渲染。实测：简单查询主调用 1769ms 返回、总结 2531ms 补全——回复提前一整轮往返显示，长推理轮省更多。
- **40 对话/滚动位置持久化 + 数据临时状态分储**：AI 页滚动位置存 sessionStorage（`scroll` 事件持续保存 + `onMounted`/`onActivated` 恢复；修复"返回不恢复"——KeepAlive 失活钩子读 scrollTop 恒 0，见关键坑）；AI 对话持久化从 localStorage 改 sessionStorage——对话是临时会话状态不是数据，与片段/收藏夹（localStorage 长期保存）区分，刷新/跳转/返回保留、关标签页清空，与编辑器草稿同策略。
- **41 AI 入口收敛（详情页删解释/优化）**：AI 助手成熟后，详情页独立的「AI 解释」（内联展开）与「AI 优化」（480px 抽屉 + diff）与助手的 summarize/modify 重叠 ~90%——双 AI 管线（prompt/解析/错误处理各一套）重复维护、面试讲不清"为什么两个入口" → 删除两面板及组件文件（AiExplainPanel.vue / AiModifyPanel.vue），详情页 AI 能力统一走「AI 助手」按钮（`?snippet` 预置当前片段为对话前提，说"解释一下/优化这段"即可）。代价：一键零输入的就地解释/优化改需在助手输入；保留 AiGeneratePanel（编辑器侧 AI 生成预览，能力面不同）。
- **40 对话/滚动位置持久化 + 数据临时状态分储**：AI 页滚动位置存 sessionStorage（`scroll` 事件持续保存 + `onMounted`/`onActivated` 恢复；修复"返回不恢复"——KeepAlive 失活钩子读 scrollTop 恒 0，见关键坑）；AI 对话持久化从 localStorage 改 sessionStorage——对话是临时会话状态不是数据，与片段/收藏夹（localStorage 长期保存）区分，刷新/跳转/返回保留、关标签页清空，与编辑器草稿同策略。

## 四、简历版要点（面试速记）

- **AI 助手（差异化核心）**：两级召回（关键词 Top 25 + 三源合并 + 属性/日期标注，成本与库规模解耦）+ 自主分析一次调用（模型读全库内容输出动作 JSON：search/summarize/modify/operate/ask/chat）+ **库操作全部放行但「AI 只提议、确认卡分级确认」**（批量删除/收藏、收藏夹 CRUD、改描述/语言，不可逆项双重确认）+ 追问范围约束（两轮编号并集）/换话题 + 推理流展示 + 错误码/日志/重试/超时体系。
- **AI 能力**：DeepSeek 统一 `chat()` 核心（SSE 流式 + 非流式共用 + 503 退避 + AbortController），封装多轮对话 / AI 生成 / 描述生成 / AI 助手（查找+分析+修改（diff 保存替换导出）+库操作提议）多个能力面；AI 能力统一走助手对话入口（详情页不留独立解释/优化面板）。
- **Monaco 二次封装**：props 化、语言映射兜底（24 语言 → monaco 语言）、**精简入口砍 4 个语言服务 worker**（TS 7MB，裁剪到 5.8MB，只留语法高亮——契合"不做 IDE"定位）+ 异步加载（defineAsyncComponent + 加载占位 + 空闲预加载）、Tab/Esc 键盘出口。
- **数据层**：Pinia + localStorage 持久化 + 一次性种子 + 布尔→多对多收藏自动迁移；派生过滤/搜索/排序（pinyin-pro 中文排序）。
- **体验**：rAF 平滑回顶 + prefers-reduced-motion；路由懒加载 + `router.isReady()` 防闪屏；侧边栏分区 + 折叠默认全展开（不持久化）；批量管理渐进式勾选；空状态动作出口；就地反馈（无 toast）。
- **面试讲法**：模型边界用**实测数据定位**（两轮规则试错 0%→0%，不是猜）；规则约束不了的**上 UI 兜底，兜底语义要物理可靠**；错误处理做"人话 + 错误码"双层（用户能行动、调试者能定位）；流式是过程性反馈不是全量阅读。
