# 优化记录（从项目搭建至今）

> 更新日期：2026-08-04
> 技术栈：Vue 3 · TypeScript · Vite · Pinia · Vue Router · Monaco Editor · TailwindCSS

项目定位：轻量化的 AI 代码片段管理平台。核心是"代码保管"，不做成正式 IDE。

---

## 一、基础功能（搭建期）

- **AI**：DeepSeek 解释代码 + AI 优化走 **SSE 流式**（`fetch + getReader()` 逐块、按行解析 `data:`、处理 `[DONE]`）→ `diffLines` 生成统一 diff（新增绿/删除红）→ 保存为新片段 / 替换原代码 / 导出。`maxTokens` 按代码长度动态计算。
- **Monaco 二次封装**：props 化（`modelValue/language/readonly/fontSize/wordWrap/frame`）、语言映射（vue→html、react→javascript…兜底 plaintext）、`v-model` + 光标事件、销毁时 `dispose()`。
- **数据层**：localStorage + `watch(deep)` 自动保存；一次性种子数据（`SEEDED_KEY`）；语言 / 收藏夹 / 最近一周（7 天内）过滤 + 标题·语言关键词搜索。
- **编辑页**：同一组件双模式（路由 name 分新建/编辑）；16 语言下拉；字号调节（14–28）；**草稿 sessionStorage + 400ms 防抖**（刷新不丢、关标签清除、带 id 校验）；未保存离开 `onBeforeRouteLeave` 拦截；文件上传按扩展名自动识别语言。
- **坑：ConfirmDialog 遮罩关闭用 `@mousedown.self`**——否则在编辑器里拖选/复制时鼠标滑过遮罩会误关弹窗。
- **详情页**：面包屑、编辑/删除（删完回首页）、id 不存在兜底重定向、`Blob` 导出（AI 优化后导出优化结果）。
- **路由**：懒加载；meta 控制布局（`hideSidebar + hideHeader` 全屏编辑）；`router.isReady()` 防刷新闪屏。

## 二、布局与体验迭代

- **列表定稿 2 列卡片网格**（`grid-cols-1 md:grid-cols-2`）。尝试过单行/双行带预览/按时间分组/主从分栏/3 列，逐个评估放弃；去代码预览（参考 Gisto/Lepton/Gists，代码进详情）。
- **返回顶部**：`requestAnimationFrame` 手写 600ms `easeOutCubic`（替代原生 smooth）；`prefers-reduced-motion` 瞬跳；检测局部滚动容器（main 是 `overflow-y-auto`）。
- **实时搜索**：输入框直接绑 `store.searchQuery`。原提交式（Enter 才写 store）导致逐字删词列表无反应，弃。
- **侧边栏**：条目化（左对齐列表项统一样式）、激活整块背景、折叠状态持久化（`code-snippets:sidebar-state`）+ 筛选时自动展开联动（保证高亮可见）。
- **批量管理（渐进式勾选，去模式化）**：卡片复选框常驻，勾选即选中，`batchMode` 为派生 computed；底部悬浮操作条随选中滑出（全选 / 按视图自适应操作：收藏→FolderPicker、移出该夹、删除带确认、清空）；批量收藏**幂等**（`folderIds` 去重）；搜索框可与批量勾选叠加使用。
- **配色加深一档**：次级文字/边框/页面底整体 +1 档。
- **分区重组**：核心动作置顶——「＋新建片段」从底部移到最顶部（用户反馈"一进来感觉不到核心在哪"）；浏览 / 筛选三分区，`border-t` + 小标题分工；「＋新建收藏夹」降级为灰字链接（与置顶"新建"撞车）。
- **排序（维度 × 方向）**：最近更新 / 最近创建 / 首字母，每维升降序。**不持久化**（用户：每次打开都该是最近更新）。**坑：首字母排序用 `localeCompare('zh')` 时 ICU 拼音 collation 把汉字拼音段整体排到拉丁字母段后**（「防抖」按 f 却排到 C/V 后）→ 引入 `pinyin-pro` 转无声调拼音与英文统一比较。**坑：方向取反逻辑对"自然方向=asc"的首字母维度取反了** → 改"正向比较 × dir"。**方向用文字代替箭头**（时间类「新→旧」、首字母「A→Z」）——箭头 ↑/↓ 有"升序 vs 最新在顶"歧义，用户反复反馈"反了"，文字零歧义。
- **空状态四场景 + 动作出口**（搜索无结果✕清空 / 无片段引导新建 / 空夹 / 无匹配查看全部）。内容区 `max-w-7xl` 居中。
- **断点适配**：2 列断点 `md(768) → lg(1024)`（768–1024 窗口 2 列挤到 232px，改单列更好）。**手机适配暂不做**（定位桌面工具，已预留小屏汉堡抽屉方案）。

## 三、多收藏夹

- **数据模型**：`isFavorited: boolean` → `folderIds: string[]`（多对多，一片段属多夹）；`Folder { id, name, createdAt }` 独立 key；**一次性迁移**旧数据（存在 true 自动建默认夹归入、剔除旧字段）。
- **交互**：卡片星弹 `FolderPicker`（勾选态切换归属，可属多夹）；侧边栏夹列表（B站式展开、内联新建/重命名、⋯菜单含编辑/清空/删除，删除不删片段只移出，删当前筛选夹回全部）；批量收藏幂等、批量移出。
- **坑：弹层关闭禁用全屏遮罩**（会遮住底层按钮→光标抽搐、hover 失效）→ document 级 `click` 监听（capture）+ 不在弹层内则 `stopImmediatePropagation` 防点击穿透；打开后**鼠标离开气泡满 5 秒**自动收起（悬停暂停计时、移出重新计、输入时停止；精确语义见十七）。
- **坑：Transition 须包在控制显隐的父级上**（`v-if` 在 Transition 内部），否则直接卸载不播 leave 动画。
- FolderPicker 通用组件：`toggle`（单选归属）/ `pick`（批量选夹）双模式。

## 四、全局 toast

- `toastStore` + `Toast.vue` 全局挂载（Teleport body），跨路由保留（保存后跳回首页仍显示）。
- **接入原则：结果"看得见"的不接**（收藏星勾选、新建/重命名夹有即时反馈）；**破坏性 / 跨页 / 无本地反馈才接**（保存/删除/清空/批量）。

> **（2026-08-03 更新）**：全局 toast 已移除，反馈改就地，见「二十、移除全局 toast」。本节只作历程保留。

## 五、模块化重构（用户："脚手架很乱"）

- 目录按职责分层：`api / components(common·layout·snippet) / editor / services / stores / types / router / views`。原则：通用→布局→业务→第三方封装。
- 拆大文件：`SortMenu`（自 SnippetList）、`SidebarFolders`（自 Sidebar，486→~150 行）、`services/storage`（种子/持久化/迁移自 store）。
- 折叠状态持久化两组件共用一个 key，**各自读全量、只改自己的字段再写回**避免互相覆盖。

## 六、长文件二次拆分（用户："代码很长")

- 纯逻辑 → `services/sort.ts`（比较函数 + 类型，store re-export 类型保引用零改动）、`fileImport.ts`、`download.ts`；`composables/useDraft.ts`（草稿 + 未保存离开确认整体移入）。
- UI 大块 → `FolderRow.vue`、`AiModifyPanel.vue`。

## 七、列表页区块化（用户："操作都在列表页但代码没有对应感"）

- **每个 UI 区块一个组件**：`ListToolbar`（图标+标题+计数+排序+搜索）/ `EmptyState`（四分支+加载中）/ `BatchActionBar`（批量条+ConfirmDialog+动画）。SnippetList 225→~50 行。
- 区块组件直接 `useSnippetStore()`（"列表页的区块"非可复用组件），装配面最薄。

## 八、Monaco 异步化

- 详情/编辑页 `MonacoEditor` 改 `defineAsyncComponent`：骨架先渲染，编辑器就绪再插入。主 chunk 410→350KB。
- **坑：`import * as monaco` 全量引入**，worker 加起来 6.6MB（ts）+2.6MB（editor.api），实际只用 8 种语言高亮——"买了用不上"。彻底裁剪需改用 esm 按需组装，风险大暂不做。

## 九、详情页重设计：轻量阅读流

- **定位：阅读页，不是工具台**。首版「顶栏 6 按钮 + 全高编辑器」被用户否掉（臃肿、AI 混在普通操作里）；改页面正常滚动、代码高度自适应（行数×22px+padding，下限 260px）、AI 解释就地内联展开、AI 优化 480px 右侧抽屉（diff 需横向空间，无遮罩）、操作沉底细栏。
- 独立全屏路由（hideSidebar/hideHeader）；顶栏统一 `BrandMark`（logo + 平台名，独立页共用）→ 用户定稿三页顶栏完全一致：品牌 + 返回，h-14。**（2026-08-03 更新：独立页顶栏右侧补页面名、返回按钮精修，见二十一。）**
- 编辑页标题行 / 底部操作栏 `mx-auto max-w-7xl` 限宽与编辑器对齐；操作按钮 `text-sm py-1.5`（text-base 大色块被否）。

## 十、简历版要点（速记）

- **AI 能力**：DeepSeek，封装普通对话与 SSE 流式请求，实现代码解释与智能优化；流式渲染 + 统一 diff，支持保存为新片段 / 替换原代码 / 导出。
- **Monaco 二次封装**：语言映射、只读/换行/字号/自动布局可配置，多页复用。
- **数据层**：Pinia + localStorage 持久化 + 一次性种子数据；计算属性派生过滤/搜索/语言统计。
- **体验优化**：rAF 平滑回顶 + `prefers-reduced-motion`；`router.isReady()` + 路由 meta 防闪屏；侧边栏分区（动作/浏览/筛选）核心动作置顶 + 折叠持久化 + 筛选联动展开；空状态动作出口 + 逐分辨率适配（断点提升至 lg）。
- **多收藏夹**：布尔升级为多对多 `folderIds`；命名夹增删改清空、卡片星弹菜单归属、幂等批量收藏 / 批量移出，旧数据自动迁移。
- **批量管理**：无"模式"切换——复选框常驻 + 底部悬浮条随勾选出现；按视图自适应，收藏夹视图支持批量移出。
- **反馈设计**：无 toast——保存后跳回首页靠列表体现，删除/批量/收藏夹操作靠页面可见变化（卡片消失/星标/计数归零）；破坏性操作由确认弹窗承担。

> **（2026-08-03 更新）**：AI 能力已扩展为「多轮对话助手 / 描述生成片段 / 意图理解搜索」，模型升级 `deepseek-v4-flash` 正式版，详情见「二十二、AI 能力升级」。AI 搜索最终形态为**两级检索**：模型解析意图（关键词/排除/语言/长度/数量）→ 本地全量字面粗召回 → 候选交模型精排；无结果如实告知原因而非兜底返回。简历讲法：*"AI 搜索采用意图解析 + 粗召回 + LLM 精排的两级检索，语义判断由模型基于片段真实内容完成"*，详见「二十三」。

---

## 十一 → 十三、配色定稿历程（关键决策）

三版配色方向迭代，**结论：产品 UI 需要至少一个强调色才能"活"**：

1. **暖纸×深青（teal）**：redesign 审计后整站重做（stone 暖中性 + teal 强调、Manrope、AppIcon 17 个内联 SVG、交互态规范、外壳 meta/favicon）。用户：「颜色不好看」→ 否。
2. **纯黑白灰 monochrome**：teal 全部映射到 neutral 墨阶。用户：「这个真是跟死人页面一样」→ 否（纯灰 = 零强调色 = 死）。
3. **墨灰×焦橙（zinc + orange）**：定稿。zinc 冷灰骨架 + 单个高饱和焦橙点睛（#C2410C）。

**定稿设计系统（详见记忆 `design-identity-zinc-orange`）**：
- 骨架 zinc：页面底 `zinc-100`、表面白、边框 `zinc-200/300`；层级阶梯（页面底 → 页上元素 zinc-200 → 白卡内 zinc-100）。
- 两档橙：`orange-700`(#C2410C) 主按钮/logo/勾选/激活文字；`orange-600`(#EA580C) 仅小图标点睛。
- **坑：对比度**——白字压 orange-600 仅 3.56:1 不达 WCAG AA；orange-700 达 5.18:1 ✓。主按钮统一 700（hover 800），图标点睛留 600。
- 语义色：`red` 仅破坏性；diff 红绿仅 DiffView。焦点环全局 `style.css` `:focus-visible` 一条。
- **坑：Windows 字体**——body 不能加 `-webkit-font-smoothing: antialiased`（关 ClearType 文字发虚显淡，曾致"页面淡像字体问题"）。字体用默认栈（Manrope 被用户否）。
- 层级 +1：hover 提一档（`zinc-100→200`）、页面底 `zinc-50→100`、卡片 shadow-sm→shadow。
- 侧边栏激活态：`bg-orange-100` + 去橙字（用户：选中不用改字体颜色，只背景变）；hover `orange-200` / active `orange-300` 三层反馈。「收藏夹」区头不参与激活（否则与子夹行双高亮挤压）。卡片 checkbox 用原生 `accent-orange-600`（用户：用原生的）。

> **（2026-08-03 更新）**：本节 orange "定稿"已被推翻，最终视觉定为 **zinc + GitHub 蓝**，见文末「十七、视觉定稿」。上面这段只作迭代历程保留。

## 十四、图标切换：阿里 iconfont（symbol 方式）

- **坑：官方 js 等 `DOMContentLoaded` 注入，与 Vue 首屏渲染竞态**，`<use>` 解析不到 symbol 空白 → 把 `<svg>` 抽到 `src/assets/iconfont.svg`，`main.ts` 用 `?raw` 读取、`app.mount` 前同步 `insertBefore` 注入。
- **坑：下载的 symbol 有 9 处硬编码 `fill="#hex"`** 不跟随主题 → 统一替换 `currentColor`。
- 雪碧图隐藏：`width:0;height:0;position:absolute;overflow:hidden`（不能 `display:none`，会破坏 symbol 解析）。
- `AppIcon.vue` `ICFONT_MAP` 语义名→iconfont 名；**star 两态**：`filled ? icon-shixinshoucang : icon-shoucang`。
- **加图标流程**：iconfont 下载 symbol → 抽取 svg 字符串（去 `window._iconfont_svg_string` 包装、fill 改 currentColor）→ 覆盖 iconfont.svg → 映射表加一行。

## 十五、图标统一收尾 + 侧边栏/按钮精修

- **零内联 SVG**：删 AppIcon 内联兜底，Toast / FolderPicker / 三处折叠箭头全换 iconfont；chevron 无现成 symbol，手写 path 补进 iconfont.svg。未映射的 name 渲染为空——6 个未接入图标（sparkles/chat/download/upload/edit/trash）的按钮只剩文字。
- 卡片多选框先试自定义 iconfont 勾 → 用户「用原生的」→ 回原生 checkbox（iconfont 对勾只用于 FolderPicker / Toast）。
- 语言标签高亮：`bg-orange-100 text-orange-800`。
- **新建片段按钮**：字不清楚 → `font-semibold` + 对比度诊断（orange-600 白字不达标）；嫌实心橙突兀 → **橙描边幽灵按钮**（白底 + `border-orange-600` + `text-orange-700`，hover `bg-orange-50`）；内容放大（text-base + 图标 18）。
- **ListToolbar 标题图标块移除**：四点缀时先上橙（`bg-orange-100`）→ 用户「标题旁边的图标不用橙色背景」→ 改回中性灰 → 再「图标没必要」→ 整块删掉（只留标题 + 计数徽章），`heading` 计算属性同步简化成纯字符串。

## 十六、收藏夹操作互斥（同一时间只允许一个操作）

- **问题**：新建输入框、行重命名、行操作菜单各自独立状态，可同时开多个（如新建收藏夹时还能编辑别的夹信息）。
- **方案**：SidebarFolders 维护 `activeRowId + creatingFolder` 互斥锁；每行 FolderRow 收 `locked` prop，被占用时 `watch` 强制关闭本行编辑态/菜单；行激活/关闭发 `activate`/`deactivate` 事件通知父组件占锁/释放。
- **规则**：新建开始 → 锁所有行并释放行锁；行激活 → 关新建输入框 + 锁其他行；行操作关闭 → 仅当该行仍持锁时释放（被锁关闭的别行不误释放当前持锁行）。

## 十七、视觉定稿：zinc + GitHub 蓝（推翻 orange）+ 收藏夹气泡化 + 交互时序

- **配色再定稿（zinc + GitHub 蓝）**：orange 版被否（用户明确"要 GitHub 那种蓝，减少颜色用量"）。骨架保持 zinc，强调色换成自定义 token：`github-blue #0969DA` 主 / `github-blue-dark #0A52B8` hover / `github-blue-light #DDF4FF` 浅底（激活态、语言标签、高亮）/ `github-blue-light-hover #C8E1FF`。全站 15+ 文件 indigo/emerald/gray → github-blue/zinc 全局替换；焦点环、选区色、favicon、`theme-color` 同步。语义色仅留 red（破坏性）+ diff 红绿（DiffView）。
- **收藏夹编辑/新建气泡化**：编辑收藏夹（FolderRow）与新建收藏夹（SidebarFolders）从"替换行的两行编辑条"改为**行下方气泡浮层**——行/按钮本身不变，白底卡片浮在下方（贴近视口底部自动翻到上方，避免被滚动区裁掉），复用 `.pop` 过渡。输入框静置淡灰底无边框，聚焦变白底 + 蓝环（用户："边框太显眼"被否）。名称输入上限 **12 字**（`maxlength=12`；20 被否——输多了输入框显示不全）；气泡加宽到撑满整行（`left-0 right-0`，~216px）保证 12 个汉字完整显示；按钮行左侧 `x/12` 字数计数。
- **自动消失时序（交互规范）**：
  - **自动收起（精确语义：鼠标离开后 5 秒）**：收藏夹操作菜单（⋯，FolderRow）、FolderPicker（卡片星弹层）。打开时起 5 秒计时；**鼠标悬停在气泡上暂停计时**（悬停多久都不收）；**移出气泡重新计 5 秒**，到点才收——不是死板的"无论动不动 5 秒就收"，而是"离开后给 5 秒缓冲"。
  - **输入型气泡不自动消失**：编辑/新建收藏夹气泡（侧边栏）与 FolderPicker 新建输入含文本框，自动收起会打断输入，刻意不做；统一走"点击气泡外 / Esc / 取消 / 保存"出口。FolderPicker 的 `scheduleAutoClose` 在 `creating`（输入中）时直接 return，完全不启动收起计时。
  - **坑：卡片弹层抖动（抽搐）**——卡片根元素 `hover:-translate-y-1 active:scale-[0.99]`（位移/缩放），弹层是卡片子元素，父级 transform 会带着弹层一起动。光标在新建输入里打字、移到相邻卡片星上时，卡片 hover 翻转 → 弹层上下位移 → 光标在弹层边缘进出成循环 = 抖动。修法：`showPicker` 打开时去掉卡片这两条，弹层开着卡片就不动。
- **气泡关闭实现**：document 级 **mousedown**（capture）监听——按下点在气泡外才关闭（不能用 click：拖选文字在气泡内按下、气泡外松开时，click 落在共同祖先上会判为"外部"误关编辑态，曾致"复制文字自动退出"），用 `.edit-bubble` / `.new-folder-bubble` 类名判断，仅在打开态拦截，不与 more 按钮的打开点击冲突；互斥锁沿用（新建↔编辑↔菜单同一时间只允许一个）。

## 十八、搜索只匹配标题 + 全站输入字数上限

- **搜索范围收紧**：`filteredSnippets` 从「标题 + 语言」两字段改为**只匹配标题**（`s.title.toLowerCase().includes(q)`，不区分大小写；先按筛选类型过滤、再套关键词）。用户："只搜标题吧"；搜索框 placeholder 同步改为「请输入片段名」。
- **全站输入字数上限**（按输入场景宽窄匹配）：
  - **收藏夹名称**（编辑/新建气泡 + FolderPicker）：12 字——窄侧边栏，12 个汉字在加宽到撑满整行（~216px）的气泡输入区完整显示；三处输入统一 `maxlength=12` + 淡灰底聚焦蓝环样式。**侧边栏两处气泡保留 `x/12` 计数**（气泡宽放得下）；**FolderPicker 内不显示计数**（输入框和「创建」按钮同行、空间窄，用户：计数样式可去，逻辑保留）。
  - **编辑器片段标题**：50 字——原本无限制，为与搜索框一致而补，保证任何标题都能被完整输入搜索到。
  - **搜索框**：50 字——只搜标题、标题通常很短（子串匹配），50 足够宽松；100 被否（无必要）。

## 十九、键盘可达性收尾

- **ConfirmDialog 全站 Esc 关闭**：5 处确认弹窗（未保存离开 / 删除片段 / 批量删除 / 清空收藏夹 / 删除收藏夹）统一支持 Esc 取消。实现：`watch(show)` 打开时挂 document 级 keydown、关闭或卸载时移除；判定 `e.key === 'Escape' && props.show`。
- **编辑器键盘离开出口**：Monaco 内嵌编辑器会把 **Tab 和 Shift+Tab 都吞掉**（分别做缩进/反向缩进），键盘用户困在编辑区出不去。初改 `tabFocusMode: true`（Tab 跳焦点）但牺牲了 Tab 缩进，用户不满 → **定稿：保留 Tab 缩进，Esc 作出口**——`editor.addCommand(KeyCode.Escape)` 扫描文档可聚焦控件，把焦点跳到编辑器之后的第一个（编辑页即字号减号 → 上传 → 保存）。与弹窗/气泡的 Esc 语义一致（"退出当前编辑"）。
- **坑：判断"下一个控件"须用 `container.contains(el)` 定位 Monaco 内部 textarea 在文档序中的位置**，再取其后第一个可聚焦元素；Monaco 的焦点层在自建 textarea 上，页面自身元素不会出现在编辑器容器内。

## 二十、移除全局 toast（反馈改就地）

- **为何移除**：toast/snackbar 本是移动端 Material 模式（屏幕挤、没地方写就地反馈）；桌面端有空间，反馈交给"结果本身可见"（卡片消失 / 星标切换 / 计数归零），GitHub 式。用户："网站要有这种提示吗，一般不都是在移动端上吗"。
- **落地**：删 `toastStore.ts` + `Toast.vue` + App.vue 挂载，7 处 `toastStore.show` 全清（保存/删除/批量收藏/批量移出/批量删除/清空夹/删夹）。
- **保存反馈（试错后定稿）**：先试「保存不跳页 + 就地『已保存 ✓』」——停留导致"保存→再改→又未保存"反复循环，且需 `createdId` 防二次保存重复新建，用户弃。**定稿：保存后直接回首页**，结果在列表可见（新建置顶 / 编辑刷新排序），无需任何提示。
- **删除 / 批量 / 收藏夹操作不再提示**：靠页面可见变化反馈；破坏性操作由 ConfirmDialog 承担确认职责。

## 二十一、视觉收尾：顶栏页面名 + 卡片 hover 加强

- **顶栏右侧补页面名**：独立页（新建/编辑/详情）顶栏右侧加当前页名——编辑页按路由显示「新建片段 / 编辑片段」，详情页「片段详情」；`ml-auto` 推右、`text-sm zinc-600`，与首页右侧标语位置对齐。
- **返回按钮精修**：从纯文字（`text-base`、无 hover 背景）改为轻量按钮——`text-sm` + `hover:bg-zinc-100` 圆角内边距 + `-ml-1` 对齐边缘，编辑页/详情页同步。
- **卡片 hover 加强**：原 `hover:shadow-md + -translate-y-0.5`（基础态已有 `shadow`，hover 差别小、反馈不明显）→ **`hover:shadow-lg + -translate-y-1 + hover:ring-github-blue/40`**（上浮 4px + 阴影加重 + 边框泛淡蓝），用户认可。

## 二十二、AI 能力升级（2026-08-03）：对话 / 生成 / 理解搜索 + 工程化加固

- **AI 层重构（`api/ai.ts`）**：收敛为统一 `chat()` 核心（非流式 / 流式共用，`stream: !!onChunk` 切换）。模型从 `deepseek-chat` → **`deepseek-v4-flash` 正式版**（`VITE_AI_MODEL` 可覆盖）；**错误不再静默**——统一抛 `AIError`（中文原因，透传 `data.error.message`），UI 显示 + 可重试；全链路 **`AbortController` 取消**（组件卸载 / 点「停止」中断流式）。原 `explainCode / generateTags` 死代码删除。
- **AI 解释 → 多轮对话助手（`AiExplainPanel` 重写）**：system 注入代码上下文（语言 + 代码 ≤12k 字符），保留 user/assistant 历史多轮追问；**流式 + markdown 渲染**；Enter 发送 / Shift+Enter 换行；流式中可「停止」；展开即自动解释一次。追问示例："为什么要用 Proxy？"。
- **Markdown 渲染（`MarkdownText.vue`）**：marked（gfm + breaks）+ **DOMPurify 净化防 XSS**；样式覆盖代码块 / 行内代码 / 标题 / 列表 / 引用 / 链接（GitHub 蓝）。
- **AI 生成新片段（`AiGeneratePanel` + 编辑页接入）**：描述 + 语言下拉 → 流式生成 → 「应用到编辑器」填充代码 / 语言，标题为空给默认名；编辑页右上角「AI 生成」按钮，面板浮在编辑器内容区右侧。
- **AI 理解搜索**：原搜索只匹配标题关键词 → AI 搜索把 query + 片段库（id/title/language/preview，前 50 条）发给模型，返回**相关 id 排序**；store 新增 `aiSearching / aiSearchIds / aiSearchError`，`filteredSnippets` 在 AI 模式下按相关度排序（不再做标题过滤）；SearchBar 加「AI」按钮 + 状态条（搜索中 / 错误重试 / 「AI 结果 N 条」+ 退出）。**坑：请求期间改关键词会写脏结果**——请求前缓存 `q`，返回时若 query 已变则丢弃（`watch(searchQuery)` 已把 `aiSearchIds` 清回 null）。
- **Bug 修复：收藏夹菜单吞掉确认弹窗第一次点击**（`FolderRow.vue`）：菜单项（清空 / 删除）动作前先收起菜单（新增 `closeMenuThen()`）。根因：菜单用 document 级**捕获** click 监听做"点击外部关闭"，删除/清空弹出 ConfirmDialog 后菜单仍保持打开，弹窗的第一次点击（点空白取消 / 点确认按钮）会被 `stopImmediatePropagation()` 吞掉，表现为"点一下没反应 / 先消失的是菜单卡片"。

## 二十三、AI 理解搜索重构（2026-08-03）：意图解析 → 严格模式 → 两级检索

本节**推翻二十二节的"模型挑 id"设计**。用户连续实测暴露问题："代码量少的"0 条、"不含注释的"返回全部、"随便输入"返回无关/全部内容，并反问"ai搜索要实现的不就是理解人的意图吗"。旧设计的根因：最多 50 条 + preview 200 字符截断，模型看不到完整内容，属性型意图（代码量/标题长短）无从判断。

- **设计一：模型只解析意图，匹配交给本地**。模型返回结构化 JSON（`keywords / exclude / language / codeLength / titleLength / limit`），本地对**全量** title+code 字面匹配打分（标题 +10 / 代码 +3），不截断。中英同义由模型补全（"防抖"→ debounce，"渐变背景"→ gradient）。`ai.ts` 新增 `SearchIntent / parseSearchIntent / executeIntentSearch`。
- **属性维度按实测逐轮补全**：
  - `limit`：用户说"两个/3个"时截断数量（兼容数字字符串 `"2"`）；没说则全量返回（用户选定：全部匹配都返回，不再限 10 条）。
  - `exclude`：否定语义（"不含/不要/排除"）。**坑：词面排除匹配不到语法注释**——代码里的注释是 `// /* */ <!-- -->`，不含"注释"二字，实测排除了个寂寞；`buildExcludePatterns` 对"注释/comment"类词追加注释语法识别，行注释用 `(^|\n)[^\S\n]*\/\/` 匹配（行首才认，避免误伤 `https://` 里的 `//`）。
  - `titleLength`：用户搜"标题最短的"0 条——模型把"标题"当成了关键词（片段里根本没有"标题"字样），且缺"按标题长度"维度。新增 titleLength 与 codeLength 区分；prompt 明确**属性词（标题/代码/长/短/几个）不进 keywords**。
  - `language` 忽略大小写：库里存 `TypeScript`，模型返回 `typescript`，严格相等 0 条。
  - 组合意图："最短的防抖" = 先关键词过滤出主题相关，再在其中按代码量排序（初版丢弃关键词直接全库排序，是 bug）。
- **严格模式（关键决策）**：去掉"关键词 0 命中退回全池"的兜底。兜底让"随便输入"返回全部/碰巧命中，**用垃圾结果掩盖了"AI 没理解"的事实**。现在：解析不出主题 → 空 + 原因 note（"没识别到搜索主题，试试描述具体内容"/"没有片段包含：xxx"），SearchBar 状态条显示原因；**属性/语言/排除本身是有效意图**（"最短的""python 的""不含注释的"），无主题关键词也正常返回。AI 搜索对**全库**执行，不再叠加当前视图过滤（否则在语言/收藏夹视图下搜索被悄悄截掉）。
- **两级检索（最终形态）**：承认"翻译→字面匹配"架构的天花板——同义词靠模型现场补全不稳定、属性维度枚举不完、模型从没见过片段内容。加第二级**模型精排**：候选 ≤15 条 + 代码 800 字符预览 + 用户原话 → 模型基于真实内容判断相关性并排序。跳过精排：候选空/单条/纯属性排序（省一次调用）；**精排失败降级返回粗召回原序**，搜索永远可用；模型认为都不相关 → 空 + "模型认为候选片段都与你的搜索不相关"。
- **坑：deepseek-v4-flash 是推理模型**（响应含 `reasoning_content`，先思考再回答）。max_tokens=100 时推理吃光额度，content 输出为空——精排 token 上限提到 400。此坑同样解释其他功能偶发返回空。
- **store / SearchBar**：新增 `aiSearchNote`（无结果原因展示），`runAiSearch` 返回 `{ ids, note }`。

## 二十四、AI 理解搜索重构（2026-08-04）：极简混合——属性机械处理 + 语义判断

**推翻二十三节的两级检索（意图解析→字面粗召回→精排）**。用户连续打补丁后点破本质："人的语言没法全部解析，字段枚举不完，描述性判断是杯水车薪——我可不可以直接让 AI 来判断，而不是我一遍遍加机制"。答复：语义准确度的天花板是模型能力，不是字段数量。字段永远只服务"机器能精确判断的部分"（客观属性），其余一律交模型看真实内容。

- **架构**：`parseIntent`（只解析属性字段）→ `applyAttributeFilters`（语言/注释/排除/收藏夹，机械过滤）→ `applyAttributeSorting`（时间/长短，本地排序）→ `semanticJudge`（候选全量 + 代码 800 字符预览 + 用户原话，模型输出相关编号）。**删除了 keywords 翻译、字面匹配打分、粗召回、精排层级、descriptive label**——纯属性查询根本不调模型。
- **属性字段定稿**：`exclude / language / codeLength / titleLength / hasComment / recent / folder / limit / hasTopic`。`hasComment`（有/无注释，行注释正则只认行首 `//` 避免误伤 `https://`）；`recent`（最近/最早添加，按 createdAt 排序）；`folder`（"我收藏的"→ 该夹内片段，按夹名匹配）；`LANGUAGE_ALIASES` 归一化（ts→TypeScript、py→python、c++→cpp）。
- **`hasTopic`（本次重构的两次试错产物）**：模型判断查询是否含主题/描述成分。第一次试错："我收藏的"过滤后 pool=1 条，仍进语义判断，模型没有收藏夹上下文判"不相关"返回空——**纯属性查询（`hasTopic=false` 且有属性条件）本地直接出结果，不劳烦模型**。第二次试错："天气真好"被模型判 `hasTopic=false`，本地直接返回全部——**属性条件全空时不信任 hasTopic，一律交语义判断兜底**（语义判断对无意义查询返回空+原因，本地返回全部是错误）。解析缺省默认 `hasTopic=true`：宁可多跑一次判断，不误放行全部。
- **结果数量控制**（用户："代码最少的出来很多个，很降低体验"）：属性排序默认 cap 3（recent 5），纯属性无排序默认 cap 5，语义判断默认展示 top 3，均带 note 告知总数（"共 N 条匹配，显示最短的前 3 条"）；明确说数量（limit）按数量。**后续补漏**：limit 最初只在属性路径生效，主题查询（"两个防抖的"）被语义判断硬编码 top 3 忽略——`semanticJudge` 接收 limit，prompt 告知模型"最多输出 N 个编号" + 本地 slice 双保险；语义判断失败降级原也漏截断（候选全返回），一并按展示上限截断。
- **回归验证**（真实 API，9 用例全过）：主题（防抖→命中）、描述（看起来像随便写的→命中垃圾数据）、属性（代码最少的/不含注释的/最近添加的/我收藏的）、组合（两个 python 的）、无意义（天气真好→空+原因）。纯属性查询只花 1 次 intent 调用；语义判断失败降级候选原序。
- **坑（保留）**：推理模型 reasoning_content 挤空 content——intent max_tokens=1000 / semantic 800，`chatRetryIfEmpty` 空输出重试一次；`isIntentEmpty`（含 hasTopic 判断）重试防走神。
- **演进路径不变**：库上千条时第一级换成向量召回，`searchSnippetsWithAI` 接口不变，第二级不动。

## 二十五、AI 助手（2026-08-04）：对话式片段检索，追问改写合并意图

**背景**：用户持续实测后点破——"现在的 AI 搜索总是出问题，把它变成主页上的 AI 助手吧"。讨论后定位到根问题：**单轮搜索的天花板是"模型理解错了，用户没有补救机会"**。产品方案：把"理解失败"变成"追问澄清"；能力范围明确为**只做搜索+追问**，不做库操作（打开/关闭/删除/收藏）——操作型 Agent 选错参数=毁数据，且"鼠标点一下比打字快"，对轻量定位是负优化（决策已记入 D5）。

- **形态**：Header 右侧「AI 助手」按钮（原标语位）→ 右侧抽屉（w-96，移动端全宽，遮罩点击关闭）。全局挂载（App.vue），任何页面可用。消息流：用户气泡（GitHub 蓝）→ 助手回复（文本 markdown + 结果小卡片：标题/语言/代码首行，点击跳详情页）。
- **核心管线 `assistantTurn`（api/ai.ts）**：历史（最近 8 条）+ 当前消息 → 模型输出动作 JSON 三选一：
  - `search`：把历史已确认条件 + 本轮消息**改写成完整自包含查询**（"防抖"+"更简单点的"→"简单点的防抖代码"）→ 复用 `searchSnippetsWithAI` 全管线。**意图解析保持单一入口，追问的意图在对话里自然累积**，不新增解析器。
  - `ask`：意图模糊（"你好""随便看看"）→ 一句话澄清问题，等用户回答；用户确认（"对""是的"）→ 按澄清方向执行 search。
  - `chat`：与找片段无关（寒暄/闲聊）→ 简短文本回复，不触发搜索。
  - 模型没给出可用 query → 兜底转 ask。
- **store（aiAssistantStore）**：open/messages/sending/error + send/reset；**10 轮上限**（20 条消息）防上下文膨胀，到顶提示「重新开始」。独立于 snippetStore，不碰搜索状态。
- **验证**（真实 API，DeepSeek 503 过载间歇跑通）：场景 A 首轮命中防抖 2 条 + 追问"更简单点的"收敛排序正确（防抖加强版在前）；B 寒暄→ask；C "随便看看"→ask→"找个防抖的"→search 命中；D 主题切换（python 排序→命中 2 条）。
- **已知边界（面试可讲）**：追问收敛依赖改写质量 + 属性解析 + 语义判断三层，主观描述（"更简单点的"）的语义判断阈值有波动（一次跑错掺入 useFetch/CSS）；DeepSeek 服务端 503 过载时整体降级。改进方向：改写 prompt 强化"属性条件必须保留在 query 里"。
- **第一版不做**（已与用户对齐）："不是这个"排除逻辑（按重搜处理）、任何库操作、工具调用。

## 二十六、删除 AI 搜索 + 响应提速（2026-08-04）

**背景**：用户实测后点破——"现在的 AI 搜索总是出问题"（响应慢：一条查询转圈十几秒；结果不稳定）。产品决策：AI 搜索整体下线，能力并入 AI 助手（第二十五节），SearchBar 回归纯关键词搜索。**AI 理解的入口从搜索框挪进对话**——单轮查询失败无补救的根问题，由对话追问解决，不再维护两个 AI 入口。

- **删除面**：SearchBar 移除 AI 按钮/状态条；snippetStore 移除 `aiSearching / aiSearchIds / aiSearchError / aiSearchNote / runAiSearch` 及关联 watch、`filteredSnippets` 的 AI 分支、`searchSnippetsWithAI` 的 import——列表页搜索只留关键词标题过滤 + 排序。
- **提速（3 次串行调用 → 2 次）**：旧助手每轮 3 次模型调用（动作判断 → parseIntent → semanticJudge），串行每轮 20s+。合并：`assistantTurn` 一次调用输出 `{"action":"search","intent":{...},"topic":"..."}`，search 动作直接复用 `parseSearchIntent` 容错解析嵌套 intent、`executeWithIntent` 执行，跳过 parseIntent 整次调用。`SEMANTIC_CODE_PREVIEW` 800→400（主题/描述判断足够，输入减半换速度）；`ASSISTANT_MAX_TOKENS` 800→1000（动作+意图一次输出，推理余量要留足）。
- **坑：嵌套 JSON 解析失败（本次最大 bug）**：search 动作的 intent 是嵌套对象，而 `tryExtractJSON` 的非贪婪正则 `\{[\s\S]*?\}` 在第一个 `}` 处截断——嵌套 JSON 永远解析失败 → 校验不过 → 兜底 ask。**表现为所有 search 查询都返回"没太听懂你想找什么"**（ask/chat 无嵌套不受影响）。改为括号配平扫描（跳过字符串内引号，处理 `\` 转义），正确提取最外层对象。
- **坑：ask 边界误判**：合并后 prompt 把"我收藏的"这类纯属性查询当意图模糊走 ask。prompt 补规则：**明确表达找片段意图（哪怕只有属性条件）不算模糊，必须走 search**。
- **真实 API 验证**（服务端正常时段）：场景 1「找两个看起来像随便写的」5.8s 命中 2 条（limit 生效）；「找防抖的代码」首轮 + 「更简单点的」追问收敛正确（防抖加强版在前）；「你好」→ ask 澄清；「两个 python 的」→ 本地路径 8s；「我收藏的」→ search + 收藏夹过滤（数据里无收藏时正确返回"没有片段"note）。**单次调用 6-12s，用户感知的"一直转圈"消失**。503 过载时段跑出的 249s 全是服务端退避重试，非代码耗时。
- **已知边界（保留）**：服务端 503 过载时重试等待决定响应时间（代码无重试，错误上抛由 UI 显示）；合并 prompt 更长，寒暄/属性边界偶发波动（已用规则约束，未到 100% 稳定——语义判断的天花板是模型能力，产品层用追问兜底）。

## 二十七、AI 助手精确性修复（2026-08-04）：语义只管相关，顺序交给本地

**背景**：用户实测——「找个代码量最少的片段」返回"全部匹配按相关度排序"；输入不相关内容时希望有提示。根因：①「找…个…片段」这类检索套话让模型把纯属性查询误判成 `hasTopic=true`，走了语义判断；②语义判断对"代码量最少"无能为力——候选全是代码片段，模型判全相关、按主观相关度返回。**语义判断的天花板是模型能力，精确的极值/顺序必须本地算**。

- **executeWithIntent 语义分支补本地重排**（关键）：语义判断结果出来后，若 intent 有排序属性（codeLength/titleLength/recent），把语义命中的候选**本地精确重排**再截断——语义只管"相关与否"，顺序/极值本地说了算。有排序属性时不把 limit 传给语义判断（让模型输出全部相关，避免"模型挑的 2 个"截断掉真正最短的），本地截断兜住数量。抽出 `sortPool / describeOrder / hasSortingAttribute` 复用纯属性路径与语义重排路径。
- **hasTopic 规则强化**（两处 prompt：parseIntent + assistantTurn）：明确"找/查/看看/有没有/个/片段"是**检索套话**不是主题；"代码量最少/代码最短/最近添加的/有注释的/python 的/两个"是**属性词**不是主题；只有真正主题词（功能名/外观描述）才 true。
- **真实 API 验证**：「找个代码量最少的片段」→ hasTopic=false 本地路径，返回真正最短 3 条 + note「共 7 条匹配，显示最短的前 3 条」（修复前是语义判断全相关+相关度顺序）；「找两个代码量最少的防抖」→ hasTopic=true 走语义筛选 + 本地重排，返回防抖 2 条按代码量序；「今天的天气怎么样」→ chat「我是代码片段助手，不查询天气哦」有提示；防抖首轮/追问/寒暄回归全过。
- **设计边界**：语义判断仍然负责"相关筛选"，其判断质量决定召回对错（模型能力天花板）；本地只保证已召回集合内的顺序与极值精确。

## 二十八、AI 助手重构成「自主分析」（2026-08-04）：删掉属性过滤层，模型读全库内容决策

**背景**：用户连续测试极简混合（二十四/二十七节）后点破本质——"你现在的设计底层还是通过属性的过滤查询，我想实现的是通过 AI 来自主分析"。"找个代码量最少的片段"被判 hasTopic 走语义判断、寒暄/套话边界反复试错（hasTopic 误判、ask 边界、嵌套 JSON、语义后重排……），本质都是**意图解析这个中间层在替模型做它不擅长的决策**。方向：删掉整套属性管线，模型自主读数据。

- **新架构**：`assistantTurn` 一次调用 = 全库片段（编号 + 语言 + 标题 + **AI 自动生成的人话描述** + 收藏夹 + 完整代码 ≤3000 字符/条）+ 对话历史 + 当前消息 → 模型输出 `{"action":"search","ids":[1,3],"note":"..."}` / ask / chat。本地只做数据准备（编号、字段拼装、JSON 校验、去重越界过滤）与渲染——**无任何过滤/排序逻辑**。删除：SearchIntent / parseIntent / applyAttributeFilters / applyAttributeSorting / semanticJudge / executeWithIntent / hasTopic / COMMENT_PATTERNS / LANGUAGE_ALIASES / limit 机制等 ~470 行（ai.ts 830→355 行）。
- **为什么"模型挑 id"这次成立**（D1 曾否定）：旧方案失败是因为 preview 200 字符 + 前 50 条截断，模型看不到完整内容，属性型意图（代码量/注释）无从判断；现在库 <50 条**全量内容直进 prompt**，且新增 `description` 字段（AI 自动生成，见上节）补上代码表达不了的背景——模型信息充足，主观/属性/组合/追问统一由它判断。
- **描述字段：AI 自动生成**（本节）：保存后异步生成（`ensureDescription` 防重复/失败静默/不阻塞跳转），详情页生成中 spinner + 失败重试按钮。
- **真实 API 验证**（全场景 2-5s，比极简混合更快——prompt 简单、无嵌套输出）：「找两个看起来像随便写的」→ 命中垃圾数据 + note「片段2本身就是随机垃圾数据」；「找个代码量最少的片段」→ 只返回 Hello World（模型自主读内容判断最短）；「找防抖的代码」→ 2 条 + 追问「更简单点的」→ 只筛基础版；「我收藏的」→ 夹内片段；「两个 python 的」→ 2 条；「你好」→ ask；「天气」→ chat。
- **权衡（接受）**：精确极值（最短/最近）从"本地机械精确"退化为"模型读内容估计"——库小场景实测准确（最短判断正确），库大了是召回层解决的事（演进不变：<50 条全量，上量后加一级召回，接口不变）。

## 二十九、AI 助手请求失败根因修复 + AI 实现全景（2026-08-04）

**背景**：用户连续报告「AI 助手请求失败，请重试」。上一轮次做了 503 退避重试、AIError 透传，均未解决——真正的根因不是服务端。

- **根因：两个 helper 函数丢失**。`assistantTurn` 调用的 `tryExtractJSON`（括号配平扫描）与 `chatRetryIfEmpty`（空输出重试一次）在整文件重写事故中只剩调用点、定义被覆盖（ai.ts 中 3 处调用、0 处定义）——每次发消息抛 `ReferenceError`（非 AIError），落在 catch 兜底文案「AI 助手请求失败，请重试」。表现为"每次都失败、永远通用文案"，与服务器状态无关，503 重试/错误透传都碰不到病根。
- **修复**：从验证通过的 verify-agent.mjs 恢复两个函数（逻辑一致，补 TS 类型 + signal 透传）；顺手修 AiAssistantPanel 的 `MarkdownText :content → :text`（prop 名不匹配，vue-tsc 报错）。`vue-tsc` 全量通过。
- **教训**：整文件 Write 重写后必须 grep 关键符号完整性 + 跑 vue-tsc 验证，"typecheck 通过"不能靠自我声明。

**AI 实现全景（截至定稿，面试可整体讲述）**：

- **统一核心 `chat()`（api/ai.ts）**：非流式/流式共用（`stream: !!onChunk`，`getReader` 按行解析 `data:`）；503 服务端过载退避重试 3 次（5s/10s/15s）；失败统一抛 `AIError`（中文原因，透传接口 `error.message`）；全链路 `AbortSignal` 取消；推理模型（deepseek-v4-flash）`reasoning_content` 挤空 content 的坑 → max_tokens 留余量 + `chatRetryIfEmpty` 空输出重试一次。
- **五个能力面**（全部只调 chat()）：
  1. **多轮对话**（chatAboutCode → AiExplainPanel）：system 注入代码上下文 ≤12k，保留 user/assistant 历史追问，流式 + Markdown（marked + DOMPurify 防 XSS）。
  2. **AI 生成片段**（generateCode → AiGeneratePanel）：描述 + 语言 → 流式 → 「应用到编辑器」。
  3. **AI 优化**（modifyCode → AiModifyPanel）：需求 → 修改后完整代码，diff 展示，保存为新片段 / 替换 / 导出。
  4. **AI 描述生成**（generateDescription + snippetStore.ensureDescription）：保存后异步、幂等（已有/生成中跳过）、失败静默不打扰 + 详情页重试按钮；生成期间被删或用户已手填则不覆盖。
  5. **AI 助手**（assistantTurn，差异化核心）：一次调用 = 全库片段（编号 + 语言 + 标题 + AI 描述 + 收藏夹 + 代码 ≤3000 字符/条）+ 最近 8 条历史 + 当前消息 → 模型输出动作 JSON 三选一：`search`（ids 编号 + note 筛选依据）/ `ask`（意图模糊澄清）/ `chat`（寒暄）。本地只做数据准备与校验：编号去重/越界过滤、括号配平 JSON 提取（嵌套 JSON，非贪婪正则解析不了）。追问在对话历史里自然累积（"更简单点的"→ 模型只在上文防抖里筛）。store 层保障：30s 超时自动中止（区分「超时」与「主动停止」两种提示）、10 轮上限、sending 防重入。
- **演进史**：模型挑 id（200 字预览内容不足 → 失败）→ 两级检索（意图解析 + 粗召回 + 精排）→ 极简混合（属性机械处理 + 语义判断）→ **自主分析（定稿：删掉全部属性管线，模型读全库内容决策）**；库上量后加一级召回，`assistantTurn` 接口不变，平滑切换。
- **边界**：库 <50 条全量直进 prompt；不做库操作（毁数据风险 + "鼠标点一下比打字快"是负优化，见 D5）；精确极值退化为模型读内容估计（库小实测准确）。

## 三十、否定/排除语义查询被 ask 兜底（2026-08-04）：推理模型 content 挤空

**背景**：用户实测「没有注释的代码」→ 助手一直回复「没太听懂你想找什么，能再说得具体一点吗？」——这是 ask 兜底默认文案，表面像"模型听不懂"，实际是输出层的问题。

- **复现（真实 API + 当前 prompt）**：3 个否定查询（「找没有注释的代码」「有没有不带注释的」「没有注释的」）× 2 次 = 6 次调用，**2 次 content 完全为空**。usage 显示 `completion_tokens` 吃满 1000 上限、`reasoning_tokens=1000`、reasoning_content 达 1800-2300 字符——**否定/排除语义让推理模型思考特别长，reasoning_content 吃光 max_tokens，content 被挤空**（D4 已记录此坑，但 1000 是按平均场景估的，低估了否定语义的推理开销）。空 content → JSON 校验失败 → 重试（约 50% 概率再空）→ ask 兜底「再说具体一点」。
- **修复**：① `ASSISTANT_MAX_TOKENS` 1000→**2000**（留足推理余量；正常查询推理 100-600 tokens 无感知，仅否定类多用）② prompt 补规则：「否定/排除表达（"没有注释的""不含 axios 的"）是明确的搜索意图，直接 search，即使可能没有匹配也返回空数组，不要用 ask 反问」。
- **验证**：同脚本重跑 6/6 全部输出合法 search JSON（推理最长 1377 tokens，<2000 余量）。模型对"哪些片段没注释"的判断有波动（一次说 1,2,3 全无、一次说仅 3）——读内容的主观判断属语义天花板，可接受。
- **教训**：推理模型的 max_tokens 预算要以**最坏推理场景**（否定/多条件/全库大）为基准，不是平均场景；"再说具体一点"这类兜底文案会掩盖"输出被挤空"的真因，排查要看 usage 里的 `reasoning_tokens`。

## 三十一、AI 助手体验改进（2026-08-04）：等待反馈 + 503 可见 + 重试按钮

**背景**：用户从使用角度指出两大痛点——① 发消息后只有"AI 正在找..."干等 6-30s+，不知道是正常分析还是卡死；② 出错/超时后要把上一条话重新打一遍。

- **等待分阶段提示**：`ChatOptions.onRetry` 回调（503 开始退避重试时触发）→ store 置 `retrying` → 面板文案：「正在分析 N 个片段…」（正常）→「服务器繁忙，自动重试中…」（503）。用户知道在等什么、能不能等。
- **503 重试不被打断**：超时计时器改为 `armTimeout()` 每次 503 重试时**重置**——重试是可恢复的明确信号，不能被"累计 30s 超时"掐断（此前 5+10+15s 退避必超 30s，用户永远只看到"搜索超时"）；但每个非重试阶段仍受 30s 兜底（网络挂起照样超时）。
- **可中断 sleep**：503 退避等待从裸 `setTimeout` 改为监听 signal 的 `sleep()`——用户点「停止」/超时立即生效，不用等 sleep 睡完（此前最长要等 15s）。
- **重试按钮**：store 记 `lastUserText` + `retry()`（先移除已展示的 user 消息再重发，避免重复展示）；面板错误区旁加「重试」小按钮（`reachedLimit` 时隐藏）。超时/失败/停止后一键重发，不用重打字。
- **验证**：vue-tsc 通过。503 时段表现需等服务端过载时实测（逻辑：重试文案出现 + 重试不被超时掐断 + 停止立即生效）。

## 三十二、换话题能力（2026-08-04）：实测定位模型边界 → 规则 + 按钮双出口

**背景**：用户指出「换话题搜」的痛点——上下文是连续累积的（追问在防抖里筛），想跳出来只能点「重新开始」清空全部。先实测模型对"换话题"表达的理解稳定性，用数据决定要不要加 UI。

- **实测方法**（真实 API）：构造 2 轮防抖上下文（历史压缩格式与线上一致）→ 7 种说法 × 3 次 = 21 次调用，判定"正确跳出"（结果指向新主题片段 3 / 无匹配时空 ids，而非继续防抖）。
- **结果（无规则基线）**：「重新找/换个话题/刚才的不算」100%；「找 CSS 的」（硬属性语言）100%；「排序的」（无关短词）67%（1 次被当闲聊）；**「不找防抖了，找渐变背景」0%**（3/3 把防抖当并列结果返回）；**「有没有渐变背景的」（无指令词）0%**（3/3 反向理解——"排除渐变背景，返回防抖"）。
- **两轮规则强化 → 复测数据有波动（重要发现）**：加"换话题忽略上文"规则后首测 4/5 依旧 0%；再加两条针对性规则（"有没有 X"＝找 X；"不找 X 了"＝放弃 X）后复测：**「不找防抖了」从 0%→100%**（3/3 正确跳出，note 明确"否定旧主题"），但 **「有没有渐变背景的」两轮 6 次稳定 0%**——**模型对规则的遵循本身有波动**（同一句式、同一规则，两次跑批结论相反），规则能提高概率、保证不了确定性。明确陈述/纯主题词/指令词句式（「找渐变背景的」「渐变背景的」「重新找…」）始终 100%，用户实测不点按钮也成功属预期。
- **结论与决策**：prompt 规则救不了语用歧义，**UI 显式出口是必须**——「换话题」按钮语义"下一条不带历史"，物理上不可能被带偏。实现：store `freshSearch` 标志（发送时 history 传空数组，用一次即复位）+ `switchTopic()` 插入「—— 新话题 ——」分割线（divider 消息仅 UI 展示，不进 prompt、不计轮数）；轮数上限口径从"消息条数/2"改为"按 user 消息计数"（分割线不占名额）；头部「换话题」与「重新开始」并列（换话题=保留记录换搜索，重新开始=清空全部）。
- **规则保留**：三条换话题规则仍留在 prompt（指令词场景 100% 稳定、无关短词 67%→100%，有保险价值；4/5 由按钮兜底）。
- **补充复测（真实线上代码，修正此前结论）**：此前 0% 结论基于复制版脚本（复制 ai.ts 逻辑 + 相同 prompt）。用 esbuild 直接打包 src/api/ai.ts 的真实 `assistantTurn`（define 注入 import.meta.env）复测 7 组 × 23 次：**「有没有渐变背景的」5/5、「库里有渐变背景的吗」3/3、「渐变背景的呢」3/3、「我想找渐变背景」3/3、「不找防抖了，找渐变背景」3/3，全部 100% 正确跳出**；回归项「更简单点的」「防抖的呢」3/3 各，正确留在防抖上下文。与用户真实使用（不点按钮也成功）一致。教训：**测试结论必须以真实线上代码为准，复制版/旧版 prompt 的结论可能失真**——之前「疑问句式稳定 0%」与真实实现不符（复制版 prompt 与线上有差异或模型波动）；按钮兜底仍保留（防波动，不依赖单次测试的乐观结论）。
- **面试讲法**：模型边界用实测数据定位（两轮规则试错 0%→0%），不是猜——"规则能约束的用规则，规则约束不了的上 UI 兜底，兜底语义要物理可靠（不带历史就不可能被带偏）"。

## 三十三、规模化准备 + 可诊断性（2026-08-05）：两级检索召回层 / 错误码日志 / 轮数与等待体验

**背景**：用户提出四个担忧——(1) 报错文案混乱，用户与调试者都无法定位根因；(2) 代码量增多后 token 成本与运行时间是否爆炸；(3) 10 轮对话 / 4 轮记忆是否太少；(4) 检索时间长的性能与提示优化。逐一落地。

**① 两级检索召回层（核心，治本）**：原设计"全库进 prompt"在库 100-200 条时必然爆 DeepSeek 上下文窗口（64K-128K）——不是成本问题，是硬天花板。改为本地零成本关键词召回 Top 25 进 prompt，成本与库规模解耦。
- **中文不分词，用 bigram 子串命中**：「渐变背景」→ 渐变/变背/背景 三组命中标题「CSS 渐变背景」，无需分词库。
- **打分加权**：标题 ×4、描述 ×3、收藏夹名 ×3、语言 ×2、代码 ×0.5（代码噪音多权重低）。
- **候选三源合并**（设计演进，最终版）：候选 = 上文搜索结果（searchIds 片段，排最前）+ query token 命中 + 最近添加兜底，去重切片 Top 25。
  - 追问（"有注释的呢""更简单点的"）语义是在上一轮结果里继续筛，**纯召回会把上文片段丢掉**——用户实测 bug：性能优化 3 条后问"有注释的呢"，返回了全库其他带注释的片段。上文片段排最前 + prompt 显式给出"上一条结果的编号范围"，模型只能在范围内筛（换话题/新主题才越界）。
  - 换话题（防抖上下文问"渐变背景"）不再靠纯召回物理排除，交给 prompt 规则（全量时代实测过正确）——实测省略式"渐变背景的呢"2/2 正确跳出。
  - **否定查询**（"没有注释的"）：被排除词"注释"命中 + 最近兜底混合——"无 X"没有文本特征可召回，只召回被排除的会让真正符合的（无注释片段）进不了候选。"有没有 X"是正反疑问不是否定，此前正则排除防止误判（统一合并后该特判自然并入三源）。
- **编号映射**：prompt 编号只对应候选集，返回时映射回真实 id。
- **组合约束的本地标注（重要）**：模型判断"有注释的 python"这类多条件组合时，纯 prompt 规则压不住——实测明知要找带注释的仍混入无注释片段（无历史"带注释的 python"仅 1/4）。改为**本地检测注释并标注候选**：`1. [Python] 数据分析脚本（含注释）`，行首正则 `/^\s*(?:\/\/|\/\*|#)|<!--/m`（行中 `#667eea` 颜色值不误报）。模型看标注判断，比自己读代码猜稳定得多——"带注释的 python"1/4 → 4/4，嵌套句式"有注释的片段里有 python 的吗"4/4。
- **收藏夹显式标注（压测发现）**：复杂压测暴露收藏夹查询硬 bug——"我收藏的"返回全部候选（note"所有片段均收藏于默认收藏"），"默认收藏里的 python"混入未收藏的片段。根因：收藏夹是行内文字且**未收藏的片段无任何标记**，模型默认全算收藏。改为候选行显式标注 `（含注释 · 收藏于：默认收藏）` / `（无注释 · 未收藏）`，"我收藏的"/"未收藏的"/"默认收藏里的 python"/"收藏在默认收藏的" 4 组 × 2 全部通过（修前 1/4）。
- **实测（真实线上代码打包，库 28-34 条 > 候选 25 条）**：「找防抖的代码」✓、「python 的」（语言召回）✓、「有没有渐变背景的」（换话题）2/2 ✓、「渐变背景的呢」（省略式换话题）2/2 ✓、「更简单点的」2/2 ✓、「没有注释的」2/2 ✓（返回全部无注释片段，note「排除带注释的片段 1 和 2」）。**追问 bug 修复前后对比**：修前「有注释的呢」（上文 3 个无注释）0/2 返回全库带注释片段 → 修后 2/2 正确返回空。残余波动：「更简单点的」偶发 1/2（模型对"没有更简单版本时返回原结果还是空"判断不稳定），属模型语义波动，非召回缺陷。
- **成本账**：召回后每轮输入固定 ~10K token（25 条 × 截断 3000 字符），不再随库线性增长；DeepSeek 自动 prompt 缓存叠加，成本与库规模解耦。

**② 错误码体系 + 本地日志（可诊断性）**：
- `AIError` 携带稳定错误码（ERR_KEY_MISSING / ERR_UNAUTHORIZED / ERR_INSUFFICIENT / ERR_RATE_LIMIT / ERR_CONTEXT / ERR_API / ERR_SERVER / ERR_NETWORK / ERR_PARSE / ERR_STREAM / ERR_FALLBACK）+ status + detail。人话文案给用户，错误码给调试者。
- store 的 error 从字符串改结构化对象；面板错误行带「详情」展开（错误码 / HTTP 状态 / 响应片段），默认收起。
- `logAiCall`：每轮 assistantTurn 记一条日志（localStorage 环形 100 条 + console）——动作 / 候选数 / prompt 字符数 / 耗时 / 错误码。调试者 F12 看 console 或读 localStorage 的 ai-call-log 即可复盘。

**③ 轮数与记忆上调**：MAX_TURNS 10→16（成本主体是候选片段不是历史，轮数翻倍成本只增约 40%）；ASSISTANT_HISTORY_LIMIT 8→12（6 轮）；面板 reachedLimit 同步 16。

**④ 等待阶段提示**：store 加 elapsed 秒计时（sending 期间每秒 +1），面板发送中文案三级：「正在分析 N 个片段…」→ 超 20 秒「AI 思考中，复杂查询约需 30-60 秒…（已等 X 秒）」→ 503 重试「服务器繁忙，自动重试中…」。让用户知道长推理不是卡死。

**⑤ 超时从 30s → 60s**（前一轮修复的延续）：推理模型对否定语义推理 1000+ token、约 40-50 秒，30s 会误杀正常慢推理（超时后重试也必超时）。

- **面试讲法**：规模化问题用数据算账定位天花板（全量 prompt 在 100-200 条爆上下文，不是猜）；召回层设计从真实失败反推（否定查询召回缺陷通过实测暴露并修复）；可诊断性做"人话 + 错误码"双层，让用户能行动、调试者能定位。

## 三十四、流式推理展示（2026-08-05）：把 DeepSeek 的 reasoning_content 流透传到面板

**背景**：AI 助手一次请求 6-50 秒，期间只有文字提示、屏幕是死的——用户不知道模型是卡了还是在想。DeepSeek 推理模型会先吐 `reasoning_content`（思考过程）再吐 `content`（最终 JSON），这个思考流可以实时展示，既缓解等待焦虑又是面试亮点（流式 + 推理模型 + 前后端配合）。

**实现**（三个文件一条链路）：
- **ai.ts**：`ChatOptions` 加 `onReasoning?: (delta: string) => void`；`readStream` 读 `delta.reasoning_content`（与 `content` 并列，思考在回答前吐完）；`chat` 的流式判断从「有 onChunk 才流式」改为「onChunk 或 onReasoning 任一存在即流式」——**关键坑：assistantTurn 只传 onReasoning 不传 onChunk，若不改，请求走非流式分支（res.text()），onReasoning 永远不触发**；`chatRetryIfEmpty` / `assistantTurn` 逐层透传。
- **store**：`reasoning` ref，send 开头置空，`onReasoning` 回调逐段 `+=`；结果返回后 sending=false 自动隐藏（reasoning 不清空，保留给以后"查看思考过程"）。
- **面板**：等待气泡内加 `AI 正在思考…` 滚动区（max-h-32 overflow-y-auto + whitespace-pre-wrap），watch reasoning 变化自动滚到底。

**实测（真实线上代码 esbuild 打包 + 真实 API）**：先用原始 fetch dump 流式响应确认字段结构——`delta` 同时含 `content`（多为 null）与 `reasoning_content`（思考文本），字段名正确。但 assistantTurn 打包测试 `reasoningChars: 0`——定位到 chat 的流式开关只看 onChunk。修复后复测：`reasoningChars: 388`、255 个 chunk 实时累积，思考内容（"我们只需要根据用户消息判断…候选片段中：1 是防抖但无注释…"）完整透传，最终 JSON 解析不受影响（action/ids/note 正常）。

**兼容性**：非推理模型不吐 reasoning_content，回调不触发，面板退化为原文字提示——向后兼容，无需开关。

**展示策略演进（针对长思考）**：初版全量滚动展示 reasoning——长思考（1000+ 字）会刷屏，且思考内容含模型中途改主意/不成熟推测，全量阅读对用户价值低、观感差。改为**尾部窗口**：流式期只显示最新 80 字 + 「已想 N 字」计数（字数增长本身即"在动"的信号，不刷屏）；结果后思考存进消息 `reasoning` 字段（不进 prompt、不影响历史），默认折叠，点「AI 思考过程（N 字）」才展开全文。目的从「让用户读推理」修正为「过程性反馈」（progressive feedback）——证明在动即可，不必证明想多深；与主流产品（Claude/DeepSeek 折叠思考）一致。

**面试讲法**：服务端已经是 SSE 流式但客户端之前等整包——"流式不只用在生成代码，推理过程也值得流式：等待期给用户过程性反馈（progressive feedback），比静态'思考中…'可信得多"；实现的关键不是读字段（一行），而是**确保只有 onReasoning 没有 onChunk 时请求也走流式分支**——这个坑用真实 API 打包测试才暴露。展示层则要克制：过程性反馈 ≠ 全量推理阅读，尾部窗口 + 折叠全文兼顾"在动"信号与长思考可读性。
